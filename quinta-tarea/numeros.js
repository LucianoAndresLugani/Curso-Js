//tarea de numeros
const alturaCm = 165;
const alturaMetros = alturaCm / 100;
const pesoKg = 60.5;
const alturaRedondeada = Math.ceil(alturaMetros);
const pesoRedondeado = Math.floor(pesoKg);
const maximoValor = Number.MAX_VALUE;
const comparacionMaximo = (maximoValor + 1) === maximoValor;
